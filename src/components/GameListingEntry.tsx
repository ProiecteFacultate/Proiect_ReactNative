import { useState } from "react";
import { Container } from "../components/BasicComponents"
import styled from "styled-components/native";
import { Text } from "react-native";

const GameListingEntry = () => {
  

  return (
    <Container>
      <Text>Game name</Text>
    </Container>
  );
}

export default GameListingEntry;